import { DaysLeftDirective } from './days-left.directive';

describe('DaysLeftDirective', () => {
  it('should create an instance', () => {
    const directive = new DaysLeftDirective();
    expect(directive).toBeTruthy();
  });
});
